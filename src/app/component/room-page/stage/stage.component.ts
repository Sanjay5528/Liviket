import { Component, Input } from '@angular/core';
import { Room, RoomState } from 'livekit-client';
// import { ControlsProps } from './ControlsView'; // Import your Angular component props interfaces
// import { ParticipantProps } from './ParticipantView'; // Import your Angular component props interfaces

@Component({
  selector: 'app-stage',
  templateUrl: './stage.component.html',
  styleUrls: ['./stage.component.css']
})
export class StageComponent {
//   @Input() room: any;
//   @Input() participantRenderer!: (props: ParticipantProps) => any;
//   @Input() controlRenderer!: (props: ControlsProps) => any;
//   @Input() onLeave!: (room: Room) => void;
//   @Input() sortParticipants!: (participants: any[]) => any[];
}
